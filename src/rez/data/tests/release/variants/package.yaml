name: bar
version: '1.0'
authors:
- joe.bloggs
description: 'a package with two variants'
variants:
- ['spangle-1.0']
- ['spangle-1.1']

private_build_requires: ["python"]

build_command: "python {root}/build.py {install}"
