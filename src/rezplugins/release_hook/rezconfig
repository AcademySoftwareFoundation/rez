emailer:
    # SMTP host.
    smtp_host: ''

    # SMTP port.
    smtp_port: 25

    # The address that post-release emails appear to come from.
    sender: '{system.user}@rez-release.com'

    # List of recipients of post-release emails.
    recipients: []

command:
    quiet: false

    on_error: 'ignore'

    # List of commands to execute prior to release, in given order.
    # Each item is a 3-uple: (command, [arguments], user).
    pre_commands: []

    # Same as above but to be executed after release.
    post_commands: []

