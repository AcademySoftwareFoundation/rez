emailer:
    # SMTP host.
    smtp_host: ''

    # SMTP port.
    smtp_port: 25

    # The address that post-release emails appear to come from.
    sender: '{system.user}@rez-release.com'

    # List of recipients of post-release emails.
    recipients: []

command:
    # List of commands to execute prior to release, in give order.
    # Each list item is a 3-uple:
    #   (command full path, [list of string arguments], needs to run as sudo (True or False)).
    pre_commands: []

    # Same as above but to be executed after release.
    post_commands: []
