language: python

matrix:
  include:
    - python: 2.7
      os: linux
      env: _REZ_SHELL=bash

install:
  - 'if [ "$_REZ_SHELL" == "tcsh" ]; then sudo apt-get install tcsh; fi'
  - 'sudo pip install .'

script:
  - 'rez selftest -s $_REZ_SHELL'
