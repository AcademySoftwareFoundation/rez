name: installation
on:
  push:
    paths:
      - 'src/**'
      - '.github/workflows/installation.yaml'

jobs:
  main:
    name: asd
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
