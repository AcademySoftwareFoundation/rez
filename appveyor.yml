image:
- Visual Studio 2015  # for Python 2.7
- Ubuntu

install:
  # Try setup.py
  - pip install . --user

  # Then try install.py
  - python install.py -v ./install

build_script:
  - echo "All ok"

build: off