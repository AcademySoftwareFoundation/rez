image:
- Visual Studio 2015  # for Python 2.7
- Ubuntu

install:
  - echo I run on both..
  - cmd: echo ..Windows..
  - sh: echo ..and Linux!

  # Try setup.py
  - pip install --user .

  # Then try install.py
  - python install.py -v ./install

test_script:
  - sh: echo $(pwd)
  - cmd: echo %cd%
  - sh: export PATH=$(pwd)/install/bin:$PATH
  - cmd: set PATH=C:\projects\bleeding-rez\install\Scripts;%PATH%
  - rez --version
  - rez env -- exit
  - rez bind --quickstart

build: off
